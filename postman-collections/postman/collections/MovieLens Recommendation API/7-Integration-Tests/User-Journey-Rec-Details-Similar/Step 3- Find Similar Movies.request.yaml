$kind: http-request
name: "Step 3: Find Similar Movies"
url: "{{baseUrl}}/v1/movies/{{first_recommended_movie_id}}/similar?n=5"
method: GET
queryParams:
  n: "5"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Find similar movies', function() {
          pm.response.to.have.status(200);
      });
order: 3000
