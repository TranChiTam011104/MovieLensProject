# API Configuration
api:
  title: "MovieLens Recommendation API"
  version: "1.0.0"
  host: "0.0.0.0"
  port: 8000
  debug: true

cors:
  allow_origins: ["*"]
  allow_credentials: true
  allow_methods: ["*"]
  allow_headers: ["*"]

recommendation:
  default_n: 10
  max_n: 50
